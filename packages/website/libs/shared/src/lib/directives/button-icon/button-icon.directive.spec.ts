import { ButtonIconDirective } from './button-icon.directive';

describe('ButtonIconDirective', () => {
    it('should create an instance', () => {
        const directive = new ButtonIconDirective(null, null);
        expect(directive).toBeTruthy();
    });
});
