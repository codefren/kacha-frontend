import { ColorFilterDirective } from './color-filter.directive';

describe('ColorFilterDirective', () => {
    it('should create an instance', () => {
        const directive = new ColorFilterDirective();
        expect(directive).toBeTruthy();
    });
});
