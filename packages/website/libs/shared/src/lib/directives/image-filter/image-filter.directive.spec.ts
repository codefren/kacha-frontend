import { ImageFilterDirective } from './image-filter.directive';

describe('ImageFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFilterDirective();
    expect(directive).toBeTruthy();
  });
});
